import React, { useState } from "react";
import Card from "@/components/ui/Card";

const Calendar = () => {
  return (
    <div>
      <Card title="Calendar">Calendar</Card>
    </div>
  );
};

export default Calendar;